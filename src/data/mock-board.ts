export type {
  BoardComment,
  BoardThread,
  BoardThreadStatus,
} from "@/types/board";

import type { BoardThread } from "@/types/board";

export const MOCK_BOARD_THREADS: BoardThread[] = [];
